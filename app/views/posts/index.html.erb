<h1>Portland ESL Network Blog</h1>

<% @posts.each do |post| %>
  <%= render partial: 'post', locals: {post: post} %>
<% end %>

<% if current_admin %>
  <%= link_to 'Add a New Post', new_admin_post_path(current_admin), :class => "btn btn-primary"  %>
<% end %>
